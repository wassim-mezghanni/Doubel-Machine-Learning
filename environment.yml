name: dml_env
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.9
  - numpy
  - pandas
  - scikit-learn
  - pytorch
  - torchvision
  - statsmodels
  - openpyxl
  - cpuonly # Change to 'pytorch-cuda' if we have GPUs
  - pip
  - pip:
    - matplotlib
    - tqdm
